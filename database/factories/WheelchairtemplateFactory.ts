import wheelchairtemplate from 'App/Models/wheelchairtemplate'
import Factory from '@ioc:Adonis/Lucid/Factory'

export default Factory.define(wheelchairtemplate, ({ faker }) => {
  return {
    //
  }
}).build()
